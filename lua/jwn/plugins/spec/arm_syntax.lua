-- ============================================================================
-- ARM assembly syntax highlighting. Pairs with the *.s -> arm filetype
-- autocmd in lua/jwn/init.lua (Neovim ships no detector for .s files;
-- the extension is ambiguous between assembler flavors).
-- ============================================================================

return {
	"ARM9/arm-syntax-vim",
}
